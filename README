# Installation
$ grails install-plugin grails-cucumber-0.2.zip

$ grails cucumber

Place Cucumber Feature definitions in baseDir/features and groovy step definitions in baseDir/features/step_definitions. Global Before and After hooks can be placed in baseDir/features/support (in groovy files).

See the wiki for more info:  http://wiki.github.com/danklynn/grails-cucumber/
